services:
  switcher:
    build: .
    container_name: sdr-switcher
    ports:
      - "8091:8091"
    env_file:
      - ../.env
    environment:
      - SDR_BASE_DIR=/opt/sdr
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ..:/opt/sdr:ro
    restart: unless-stopped
