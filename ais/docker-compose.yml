services:
  ais:
    build: .
    container_name: ais
    restart: unless-stopped
    privileged: true
    ports:
      - "8090:8090"
    devices:
      - /dev/bus/usb:/dev/bus/usb
    environment:
      TZ: Europe/Istanbul
    tmpfs:
      - /tmp
